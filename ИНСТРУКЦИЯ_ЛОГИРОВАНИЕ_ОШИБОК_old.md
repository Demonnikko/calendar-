# 🔔 ИНСТРУКЦИЯ: Логирование ошибок в Telegram

Эта система автоматически отправляет вам в Telegram уведомления когда у пользователя калькулятора происходит ошибка.

## 📋 ЧТО СДЕЛАТЬ

### 1️⃣ Узнать свой Telegram ID

1. Откройте терминал в папке `ТЕЛЕГРАМ БОТ /`
2. Запустите команду:
   ```bash
   node get_my_id.js
   ```
3. Откройте Telegram, найдите своего бота
4. Напишите боту `/start`
5. Он пришлет ваш ID (например: `123456789`)

### 2️⃣ Вставить свой ID в config.js

1. Откройте файл `ТЕЛЕГРАМ БОТ /config.js`
2. Найдите строку:
   ```javascript
   const ADMIN_ID = 0; // ← ЗАМЕНИТЕ НА ВАШ ID!
   ```
3. Замените `0` на ваш ID:
   ```javascript
   const ADMIN_ID = 123456789; // ← Ваш настоящий ID
   ```
4. Сохраните файл

### 3️⃣ Перезапустить бота

1. Остановите текущий бот (Ctrl+C в терминале где он запущен)
2. Запустите заново:
   ```bash
   cd "ТЕЛЕГРАМ БОТ "
   node index.js
   ```

### 4️⃣ Добавить код в Калькулятор

1. Откройте файл `КАЛЬКУЛЯТОР /index.html`
2. Найдите строку **2306** (внутри `<script>` тега)
3. Скопируйте ВЕСЬ КОД из файла `КАЛЬКУЛЯТОР /error_logging.js`
4. Вставьте его сразу после строки 2306 (после `<script>`)
5. Сохраните файл

## ✅ ГОТОВО!

Теперь когда у пользователя произойдет ошибка - вы АВТОМАТИЧЕСКИ получите сообщение в Telegram:

```
🚨 ОШИБКА В КАЛЬКУЛЯТОРЕ

Тип: JavaScript Error
Ошибка: Cannot read property 'value' of null
Файл: https://your-site.com/index.html
Строка: 4523
Устройство: Mozilla/5.0 (iPhone; CPU iPhone OS 16_0...)
Время: 08.02.2026, 14:32:15
```

## 🎯 ДОПОЛНИТЕЛЬНО: Логирование событий

Вы также можете логировать важные события (активация, создание КП):

### В index.html найдите строку ~8717 (активация лицензии):
```javascript
localStorage.setItem('illusionist_license', encoded);

// ДОБАВЬТЕ ЭТУ СТРОКУ ↓
window.logAppEvent('license_activated', {
  key: key.substring(0, 4) + '****'
});

console.log('✅ License activated locally');
```

### В index.html найдите строку ~5494 (создание КП):
```javascript
App.showToast(success ? 'КП скопировано!' : 'Не удалось скопировать');

// ДОБАВЬТЕ ЭТИ СТРОКИ ↓
if (success) {
  window.logAppEvent('kp_created', {
    total: State.finalTotal,
    items: State.cart.length
  });
}
```

Тогда вы будете получать такие уведомления:

```
📊 СОБЫТИЕ В КАЛЬКУЛЯТОРЕ

Событие: license_activated
Данные:
  • key: ABCD****
Время: 08.02.2026, 14:30:12
```

## 🔒 БЕЗОПАСНОСТЬ

- ✅ Только **ВЫ** (админ) видите эти сообщения
- ✅ Обычные пользователи бота **не видят** ошибки
- ✅ Пользователи калькулятора **не видят** что ошибки логируются
- ✅ Токен бота защищен (доступен только в вашем коде)

## ❓ FAQ

**Q: Что если я забуду вставить ADMIN_ID?**
A: Ничего страшного - ошибки просто не будут отправляться. Проверяйте логи бота - там будет предупреждение.

**Q: Будут ли пользователи видеть что я получаю ошибки?**
A: Нет! Все происходит тихо и незаметно.

**Q: Сколько это стоит?**
A: Бесплатно! Telegram Bot API бесплатный без ограничений.

**Q: Можно ли добавить еще админов?**
A: Да! В index.js можно сделать массив ADMIN_IDS и отправлять всем.

---

**Готово! Теперь вы будете в курсе всех проблем пользователей.** 🎉
