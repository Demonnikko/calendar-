# 📳 ИНСТРУКЦИЯ: Вибрация для календаря

## ✅ Что добавлено

Создан **умный код вибрации** для всех важных действий в календаре с **особым акцентом** на клики по датам!

### 🎯 Когда вибрирует:

1. **КЛИК ПО ДАТЕ В КАЛЕНДАРЕ** → **Двойной импульс (20-30-20 мс)** 🗓️ ⭐
2. **"Сохранить событие"** → Паттерн успеха (два импульса) ✅
3. **"Удалить событие"** → Средняя вибрация 🗑️
4. **"Создать событие" (+)**  → Легкая вибрация ➕
5. **Переключение месяцев** → Легкая вибрация ⬅️➡️
6. **Переключение табов** → Средняя вибрация 📊
7. **Раскрытие секций** → Легкая вибрация 📂
8. **Закрытие модальных окон** → Легкая вибрация ✕
9. **Любые уведомления (toast)** → Автоматическая вибрация

---

## 🍎 Работа на iPhone

**ВАЖНО:** На iPhone вибрация работает **ТОЛЬКО** когда приложение добавлено на домашний экран!

### Как активировать:
1. Открыть календарь в Safari
2. Нажать "Поделиться" (квадрат со стрелкой вверх)
3. Выбрать **"На экран Домой"**
4. Открыть приложение с домашнего экрана
5. **Готово!** Теперь клики по датам вибрируют 📳

---

## 📱 Android

На Android работает сразу, даже в обычном браузере Chrome.

---

## 🎨 Типы вибрации

| Действие | Тип | Длительность |
|----------|-----|--------------|
| **Клик по дате** | **Двойной импульс** | **20-30-20 мс** ⭐ |
| Сохранить событие | Паттерн успеха | 15-50-15 мс |
| Удалить событие | Средняя | 25 мс |
| Открыть модалку | Легкая | 15 мс |
| Переключить месяц | Легкая | 15 мс |
| Переключить таб | Средняя | 25 мс |
| Ошибка | Паттерн | 10-50-10-50-10 мс |

---

## 📋 Установка

### 1. Откройте файл календаря

Откройте `КАЛЕНДАРЬ/Финал/index.html`

### 2. Найдите конец script тега

Прокрутите в самый конец файла (примерно строка 7000+) и найдите закрывающий тег `</script>`

### 3. Вставьте код

Скопируйте **ВЕСЬ КОД** из файла `КАЛЕНДАРЬ/haptic_feedback.js` и вставьте его **ПЕРЕД** закрывающим `</script>`

```html
<!-- index.html -->
<script>
  // ... весь существующий код приложения ...

  // ============================================
  // ТАКТИЛЬНАЯ ВИБРАЦИЯ ДЛЯ КАЛЕНДАРЯ
  // ============================================
  (function() {
    // КОД ВИБРАЦИИ СЮДА
  })();

</script> <!-- ← ПЕРЕД ЭТИМ ТЕГОМ -->
```

### 4. Сохраните и проверьте

1. Сохраните файл
2. Обновите страницу в браузере (F5 или Cmd+R)
3. **Кликните на дату в календаре** - должна быть **двойная вибрация**!
4. Создайте и сохраните событие - вибрация при сохранении

**На iPhone:** Добавьте приложение на домашний экран и откройте оттуда.

---

## 🔧 Как это работает

Код делает две вещи:

### 1. Перехватывает функции календаря
```javascript
// Перехватываем selectDate для вибрации при клике на дату
window.selectDate → ДВОЙНАЯ ВИБРАЦИЯ → оригинальная функция

// Перехватываем saveEvent
window.saveEvent → Паттерн успеха → оригинальная функция

// Перехватываем deleteEvent
window.deleteEvent → Средняя вибрация → оригинальная функция
```

### 2. Перехватывает уведомления
```javascript
// Когда появляется toast - автоматическая вибрация
showToast('Сохранено!') → Паттерн успеха
showToast('Ошибка!') → Паттерн ошибки
```

---

## ⚙️ Настройка интенсивности

Можно изменить длительность вибрации в коде:

```javascript
const HAPTIC = {
  light: 15,       // ← Легкая
  medium: 25,      // ← Средняя
  strong: 35,      // ← Сильная
  date: [20, 30, 20],  // ← ДЛЯ КЛИКОВ ПО ДАТЕ! (можно сделать [25, 40, 25] для сильнее)
  success: [15, 50, 15],  // ← Успех
  error: [10, 50, 10, 50, 10]  // ← Ошибка
};
```

**Рекомендации:**
- Не делайте больше 50 мс (будет слишком сильно)
- Для клика по дате рекомендуется `[20, 30, 20]` - приятный двойной импульс

---

## 🎯 Дополнительные возможности

### Вызвать вибрацию вручную из любого места кода:

```javascript
// Легкая вибрация
window.hapticFeedback('light');

// Средняя
window.hapticFeedback('medium');

// Сильная
window.hapticFeedback('strong');

// Специальный паттерн для выбора даты
window.hapticDateSelect();

// Паттерн успеха
window.hapticSuccess();

// Паттерн ошибки
window.hapticError();
```

### Пример использования:

```javascript
// При своем кастомном действии
function myCustomFunction() {
  window.hapticFeedback('medium');
  // Ваш код...
}
```

---

## 🌟 Особенность: Клик по датам

Самая важная фишка - **двойная вибрация при клике на дату календаря**!

Код перехватывает функцию `selectDate()` которая вызывается при клике на ячейку календаря, и добавляет **особый паттерн вибрации** `[20, 30, 20]` - два быстрых импульса с паузой.

Это создает **приятный тактильный отклик** который ощущается как "клик" настоящей кнопки. Особенно круто на iPhone в PWA режиме!

---

## ❓ FAQ

**Q: Почему на моем iPhone не работает?**
A: Приложение должно быть добавлено на домашний экран! Safari → Поделиться → "На экран Домой"

**Q: Как отключить вибрацию?**
A: Удалите код из index.html

**Q: Можно ли сделать вибрацию при клике по дате сильнее?**
A: Да! Измените `date: [20, 30, 20]` на `date: [25, 40, 25]` или `date: [30, 50, 30]`

**Q: Почему вибрирует при каждом уведомлении?**
A: Код перехватывает showToast и автоматически добавляет вибрацию. Это удобно!

**Q: Работает ли на iPad?**
A: Да, но только в PWA режиме (после добавления на домашний экран)

**Q: Разряжает ли батарею?**
A: Нет, вибрация очень короткая (15-35 мс), практически не влияет на батарею

---

## 🎉 Готово!

Теперь ваш календарь будет:
- ✅ Вибрировать при **каждом клике на дату** (двойной импульс!)
- ✅ Вибрировать при сохранении событий
- ✅ Вибрировать при удалении
- ✅ Вибрировать при всех важных действиях

**Особенно круто на iPhone** после добавления на домашний экран - ощущения как от настоящего нативного iOS приложения! 🍎✨
