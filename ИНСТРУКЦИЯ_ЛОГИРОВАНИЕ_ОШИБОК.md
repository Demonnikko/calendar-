# 🔔 ИНСТРУКЦИЯ: Логирование ошибок календаря в Telegram

Эта система автоматически отправляет вам в Telegram уведомления когда у пользователя календаря происходит ошибка.

## 📋 ЧТО СДЕЛАТЬ

### ✅ Бот уже настроен!

Telegram бот уже настроен и готов принимать ошибки:
- ADMIN_ID установлен: **269357522** (Дмитрий Костюк)
- Бот запущен и работает
- Обработчики ошибок обновлены для различения приложений

### 1️⃣ Добавить код в Календарь

1. Откройте файл `КАЛЕНДАРЬ/Финал/index.html`
2. Найдите **строку 50** (внутри первого `<script>` тега, сразу после конфигурации Firebase)
3. Скопируйте **ВЕСЬ КОД** из файла `КАЛЕНДАРЬ/error_logging.js`
4. Вставьте его сразу после строки 88 (после функции `initFirebase()`)
5. Сохраните файл

**Где вставить:**
```html
<script>
  // ============================================
  // FIREBASE CONFIGURATION
  // ============================================
  const FIREBASE_CONFIG = { ... };

  function initFirebase() { ... }

  // ← ВСТАВИТЬ КОД СЮДА (после initFirebase)

</script>
```

### 2️⃣ Перезапустить бот (если не запущен)

```bash
cd "ТЕЛЕГРАМ БОТ "
node index.js
```

## ✅ ГОТОВО!

Теперь когда у пользователя календаря произойдет ошибка - вы АВТОМАТИЧЕСКИ получите сообщение в Telegram:

```
🚨 ОШИБКА В КАЛЕНДАРЕ 📅

Тип: JavaScript Error
Ошибка: Cannot read property 'value' of null
Файл: https://your-site.com/index.html
Строка: 2456
Устройство: Mozilla/5.0 (iPhone; CPU iPhone OS 16_0...
Время: 08.02.2026, 14:32:15
```

## 🎯 РАЗЛИЧИЕ ПРИЛОЖЕНИЙ

Бот теперь **различает** ошибки из разных приложений:

- **Калькулятор:** `🚨 ОШИБКА В КАЛЬКУЛЯТОРЕ 🧮`
- **Календарь:** `🚨 ОШИБКА В КАЛЕНДАРЕ 📅`

Так вы сразу поймете откуда пришла ошибка!

## 🎯 ДОПОЛНИТЕЛЬНО: Логирование событий

Вы также можете логировать важные события в календаре (создание события, удаление и т.д.):

### Найдите функцию saveEvent (строка ~5657)

```javascript
function saveEvent() {
  const title = document.getElementById('event-title').value.trim();
  const date = document.getElementById('event-date').value;

  // ... существующий код ...

  // ДОБАВЬТЕ ЭТИ СТРОКИ ПОСЛЕ УСПЕШНОГО СОХРАНЕНИЯ ↓
  window.logAppEvent('event_created', {
    title: title.substring(0, 20) + '...',
    date: date
  });
}
```

### Найдите функцию deleteEvent (строка ~5858)

```javascript
function deleteEvent() {
  if (!editingEventId) return;

  // ... существующий код удаления ...

  // ДОБАВЬТЕ ЭТУ СТРОКУ ПОСЛЕ УДАЛЕНИЯ ↓
  window.logAppEvent('event_deleted', {
    id: editingEventId
  });
}
```

Тогда вы будете получать такие уведомления:

```
📊 СОБЫТИЕ В КАЛЕНДАРЕ 📅

Событие: event_created
Данные:
  • title: День рождения клиента...
  • date: 2026-03-15
Время: 08.02.2026, 14:30:12
```

## 🔒 БЕЗОПАСНОСТЬ

- ✅ Только **ВЫ** (админ) видите эти сообщения
- ✅ Обычные пользователи бота **не видят** ошибки
- ✅ Пользователи календаря **не видят** что ошибки логируются
- ✅ Токен бота защищен (доступен только в вашем коде)

## ❓ FAQ

**Q: Будут ли пользователи видеть что я получаю ошибки?**
A: Нет! Все происходит тихо и незаметно.

**Q: Как отличить ошибку календаря от калькулятора?**
A: В сообщении будет написано "В КАЛЕНДАРЕ 📅" или "В КАЛЬКУЛЯТОРЕ 🧮"

**Q: Сколько это стоит?**
A: Бесплатно! Telegram Bot API бесплатный без ограничений.

**Q: Можно ли отключить логирование?**
A: Да, просто удалите код из index.html календаря

---

**Готово! Теперь вы будете в курсе всех проблем пользователей в обоих приложениях.** 🎉
